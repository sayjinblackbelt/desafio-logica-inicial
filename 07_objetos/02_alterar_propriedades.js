// 02 — Acessando e alterando propriedades

const computador = {
  marca: "Lenovo",
  memoriaGB: 24,
  ligado: true
};

computador.memoriaGB = 32;
computador.sistema = "Windows";

console.log(computador);
